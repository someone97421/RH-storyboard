# 领域知识文档 (Domain docs)

**【全局指令】**：在处理此代码库的所有任务时，智能体**必须**使用中文进行内部思考、对话和交流。

本项目是一个**单上下文 (single-context)** 代码库。

- **Context map (上下文映射)**: 未使用。
- **Domain model (领域模型)**: 请阅读根目录下的 `CONTEXT.md` 以了解业务领域词汇、惯用语和项目整体目标。
- **Decisions (架构决策)**: 请阅读 `docs/adr/` 了解历史架构决策记录 (ADRs)。
